# Spring Boot 应用名称
spring.application.name=project-tachy

# ✅ PostgreSQL 数据源配置
spring.datasource.url=jdbc:postgresql://38.22.90.74:5432/raven_db
spring.datasource.username=jimei
spring.datasource.password=jimei
spring.datasource.driver-class-name=org.postgresql.Driver

# ✅ Flyway 配置（数据库迁移）
spring.flyway.locations=classpath:db/migration
spring.flyway.enabled=true

# ✅ Hibernate 配置（仅校验，不创建/更新）
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# ✅ OpenAI Chat 模型配置（⚠️记得换成你自己的 key）
spring.ai.openai.api-key=sk-7dadf5c894664d9abd339cc95dd84697
# 你也可以用 chat.api-key
spring.ai.openai.chat.base-url=https://dashscope.aliyuncs.com/compatible-mode/
spring.ai.openai.chat.completions-path=/v1/chat/completions
# ✅ 可选：OpenAI 模型设置（如使用 GPT-4）
spring.ai.openai.chat.options.model=qwen-turbo-latest
# spring.ai.openai.chat.options.temperature=0.7

# ✅ 其他常见配置
server.port=3001
